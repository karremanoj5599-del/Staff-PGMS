{
  "buildCommand": "npx expo export",
  "outputDirectory": "dist",
  "devCommand": "npx expo start --web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
